<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        .container{
            display: flex;
            justify-content: center;
            flex-direction: column;
            align-items: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>CARD LIST</h2>
        <br>
        <a href="/card/cardRegister">카드 등록하기</a>
        <table>
            <tr>
                <th>카드 번호</th>
                <th>유효 기간</th>
                <th>카드 종류</th>
            </tr>
            <% for(let i=0; i<cards.length; i++) { %>
                <tr>
                    <td><%= cards[i].card_num %></td>
                    <td><%= cards[i].card_valid %></td>
                    <td><%= cards[i].card_type %></td>
                </tr>
            <% } %>
        </table>
    </div>
</body>
</html>