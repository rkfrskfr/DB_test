<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        .container{
            display: flex;
            justify-content: center;
            flex-direction: column;
            align-items: center;
        }
    </style>
</head>
<body>
    <%- include('../navbar', { user_id: user_id }) %>
    <div class="container">
        <h3>주문하기</h3>
        <form action="/order/orderCart" method="post">
            <table>
                <tr>
                    <th>책 제목</th>
                    <th>수량</th>
                    <th>가격</th>
                </tr>
                <tbody>
                    <% for(let i=0; i<cartlist.length; i++) { %>
                        <tr>
                            <td><%= cartlist[i].book_name %></td>
                            <td><%= cartlist[i].quantity %></td>
                            <td><%= cartlist[i].price * cartlist[i].quantity %></td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
            <br>
    
            <h4>총 금액: <%= totalP %></h4>
            <br>
    
            <p>카드 선택하기</p>
            <select name="card_num">
                <option disabled hidden selected>카드를 선택해주세요</option>
                <% for(let i=0; i<cardlist.length; i++) { %>
                    <option value="<%= cardlist[i].card_num %>" key="<%= cardlist[i].card_num %>"><%= cardlist[i].card_num %></option>
                <% } %>
            </select>
    
            <p>주소 선택하기</p>
            <select name="postal_code">
                <option disabled hidden selected>주소를 선택해주세요</option>
                <% for(let i=0; i<addresslist.length; i++) { %>
                    <option value="<%= addresslist[i].postal_code %>" key="<%= addresslist[i].postal_code %>"><%= addresslist[i].postal_code %></option>
                <% } %>
            </select>
            <br>
            <button type="submit">주문하기</button>
        </form>
    </div>
</body>
</html>